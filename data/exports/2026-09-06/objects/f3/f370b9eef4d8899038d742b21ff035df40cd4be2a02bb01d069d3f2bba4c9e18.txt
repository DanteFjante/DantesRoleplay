{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "section", "order", "title", "summary", "blocks", "examples",
    "relatedRuleRefs", "citations", "mechanicIds", "procedureIds",
    "visibility", "presentationStatus"
  ],
  "properties": {
    "section": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "label", "order"],
      "properties": {
        "id": { "$ref": "#/$defs/slug" },
        "label": { "$ref": "#/$defs/label" },
        "order": { "$ref": "#/$defs/order" }
      }
    },
    "order": { "$ref": "#/$defs/order" },
    "title": { "$ref": "#/$defs/title" },
    "summary": { "$ref": "#/$defs/summary" },
    "blocks": {
      "type": "array", "minItems": 1, "maxItems": 64,
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kind", "heading", "body", "items"],
        "properties": {
          "kind": { "type": "string", "enum": ["paragraph", "steps", "list", "callout"] },
          "heading": { "type": ["string", "null"], "minLength": 1, "maxLength": 200, "pattern": "\\S" },
          "body": { "type": ["string", "null"], "minLength": 1, "maxLength": 10000 },
          "items": {
            "type": "array", "maxItems": 64,
            "items": { "type": "string", "minLength": 1, "maxLength": 1000, "pattern": "\\S" }
          }
        }
      }
    },
    "examples": {
      "type": "array", "maxItems": 32,
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["title", "body"],
        "properties": {
          "title": { "$ref": "#/$defs/title" },
          "body": { "type": "string", "minLength": 1, "maxLength": 5000 }
        }
      }
    },
    "relatedRuleRefs": {
      "type": "array", "maxItems": 32,
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["entityId"],
        "properties": { "entityId": { "$ref": "#/$defs/qualifiedId" } }
      }
    },
    "citations": {
      "type": "array", "minItems": 1, "maxItems": 32,
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["sourceId", "locator"],
        "properties": {
          "sourceId": { "type": "string", "minLength": 1, "maxLength": 200, "pattern": "\\S" },
          "locator": { "type": "string", "minLength": 1, "maxLength": 1000, "pattern": "\\S" }
        }
      }
    },
    "mechanicIds": {
      "type": "array", "maxItems": 32, "uniqueItems": true,
      "items": { "$ref": "#/$defs/qualifiedId" }
    },
    "procedureIds": {
      "type": "array", "maxItems": 32, "uniqueItems": true,
      "items": { "$ref": "#/$defs/qualifiedId" }
    },
    "visibility": { "type": "string", "enum": ["public", "dm"] },
    "presentationStatus": { "type": "string", "enum": ["draft", "published", "retired"] }
  },
  "$defs": {
    "slug": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$", "maxLength": 100 },
    "label": { "type": "string", "minLength": 1, "maxLength": 160, "pattern": "\\S" },
    "title": { "type": "string", "minLength": 1, "maxLength": 200, "pattern": "\\S" },
    "summary": { "type": "string", "minLength": 1, "maxLength": 2000 },
    "order": { "type": "integer", "minimum": 0, "maximum": 10000 },
    "qualifiedId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*(?:\\.[a-z][a-z0-9-]*)+$",
      "maxLength": 400
    }
  }
}