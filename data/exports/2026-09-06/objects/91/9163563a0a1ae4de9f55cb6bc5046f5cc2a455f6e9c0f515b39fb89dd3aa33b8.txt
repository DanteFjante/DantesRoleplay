<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="A private Dantes dashboard for local conversations, notes, and the present moment.">
  <title>Dantes · Home</title>
  <style>
    :root { color-scheme: dark; --ink: #edf7e8; --muted: #a6c3a1; --faint: #71936f; --line: rgba(151,214,137,.2); --leaf: #9fdb8f; --leaf-deep: #3b9b5c; --soil: #08150e; --surface: rgba(12,34,23,.78); --danger: #e9a58b; }
    * { box-sizing: border-box; }
    body { min-height: 100vh; margin: 0; color: var(--ink); background: radial-gradient(circle at 86% -8%, rgba(93,184,94,.22), transparent 31rem), radial-gradient(circle at 6% 84%, rgba(37,115,68,.25), transparent 34rem), var(--soil); font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }
    body::before, body::after { position: fixed; z-index: -1; width: 32rem; height: 32rem; pointer-events: none; content: ""; opacity: .34; background: radial-gradient(ellipse 10px 20px at 25% 20%, var(--leaf) 0 44%, transparent 47%), radial-gradient(ellipse 9px 19px at 48% 43%, var(--leaf-deep) 0 44%, transparent 47%), radial-gradient(ellipse 11px 21px at 68% 27%, #75c773 0 44%, transparent 47%), radial-gradient(ellipse 8px 17px at 81% 65%, #4ca766 0 44%, transparent 47%), repeating-linear-gradient(112deg, transparent 0 35px, rgba(142,211,119,.55) 36px 38px, transparent 39px 74px); filter: blur(.2px); }
    body::before { top: -13rem; right: -12rem; transform: rotate(18deg); }
    body::after { bottom: -14rem; left: -13rem; transform: rotate(194deg); }
    a, button, select, textarea { font: inherit; }
    button, select, textarea { color: inherit; }
    .shell { width: min(1180px, calc(100% - 40px)); margin: 0 auto; padding: 28px 0 64px; }
    .topbar { display: flex; align-items: center; justify-content: space-between; gap: 20px; padding-bottom: 30px; }
    .brand { display: flex; align-items: center; gap: 12px; }
    .brand { color: var(--ink); font-weight: 760; letter-spacing: .02em; text-decoration: none; }
    .mark { display: grid; width: 34px; height: 34px; place-items: center; border: 1px solid var(--line); border-radius: 50% 50% 46% 54%; color: var(--leaf); background: linear-gradient(140deg, rgba(159,219,143,.18), rgba(21,53,37,.78)); box-shadow: inset 0 0 20px rgba(159,219,143,.08); }
    .quiet-link { padding: 8px 12px; border: 1px solid transparent; border-radius: 999px; color: var(--muted); font-size: .88rem; text-decoration: none; }
    .quiet-link:hover, .quiet-link:focus-visible { border-color: var(--line); color: var(--ink); outline: none; }
    .game-link { display: inline-flex; align-items: center; gap: 9px; margin-top: 24px; padding: 12px 16px; border: 1px solid rgba(159,219,143,.56); border-radius: 999px; color: #f1ffe9; background: linear-gradient(135deg, rgba(96,171,82,.45), rgba(24,74,42,.72)); box-shadow: 0 12px 30px rgba(0,0,0,.18); font-weight: 800; text-decoration: none; }
    .game-link::after { content: "→"; color: var(--leaf); font-size: 1.1rem; }
    .game-link:hover, .game-link:focus-visible { border-color: var(--leaf); background: linear-gradient(135deg, rgba(112,195,93,.56), rgba(24,74,42,.82)); outline: none; transform: translateY(-1px); }
    system-navigation { --system-navigation-link-color: var(--muted); --system-navigation-muted-color: var(--faint); --system-navigation-border-color: var(--line); --system-navigation-current-border-color: var(--line); --system-navigation-current-background: rgba(96,171,82,.15); }
    outer-ai, inner-ai { --ai-color: var(--ink); --ai-muted-color: var(--muted); --ai-border-color: var(--line); --ai-error-color: #ff9b8f; }
    .hero { display: grid; grid-template-columns: minmax(0,1fr) minmax(240px,.46fr); gap: clamp(26px,7vw,92px); align-items: end; padding: 32px 0 46px; }
    .eyebrow { margin: 0 0 15px; color: var(--leaf); font-size: .74rem; font-weight: 800; letter-spacing: .18em; text-transform: uppercase; }
    h1, h2, p { margin-top: 0; }
    h1 { max-width: 760px; margin-bottom: 17px; font-family: Georgia, "Times New Roman", serif; font-size: clamp(3.2rem,7.8vw,6.5rem); font-weight: 500; letter-spacing: -.055em; line-height: .9; }
    h1 em { color: var(--leaf); font-style: normal; }
    .lead { max-width: 620px; margin-bottom: 0; color: var(--muted); font-size: 1.08rem; line-height: 1.65; }
    .clock { min-height: 178px; padding: 24px; border: 1px solid var(--line); border-radius: 20px; background: linear-gradient(145deg, rgba(48,113,63,.24), var(--surface)); box-shadow: 0 20px 70px rgba(0,0,0,.2); }
    .clock small, .card-kicker { display: block; color: var(--faint); font-size: .7rem; font-weight: 800; letter-spacing: .14em; text-transform: uppercase; }
    #local-date-time { display: block; margin-top: 20px; color: var(--leaf); font-family: ui-monospace, SFMono-Regular, Consolas, monospace; font-size: clamp(1.3rem,2.4vw,2rem); line-height: 1.3; }
    .dashboard { display: grid; grid-template-columns: minmax(0,1.45fr) minmax(285px,.78fr); gap: 18px; align-items: start; }
    .card { overflow: hidden; border: 1px solid var(--line); border-radius: 19px; background: var(--surface); box-shadow: 0 18px 60px rgba(0,0,0,.16); }
    .card-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 20px; padding: 25px 26px 20px; border-bottom: 1px solid var(--line); background: linear-gradient(90deg, rgba(84,161,79,.1), transparent 60%); }
    h2 { margin-bottom: 7px; font-family: Georgia, "Times New Roman", serif; font-size: 1.65rem; font-weight: 500; }
    .card-head p, .note-copy { margin-bottom: 0; color: var(--muted); font-size: .9rem; line-height: 1.55; }
    .local-badge { flex: 0 0 auto; color: var(--leaf); font-size: .76rem; font-weight: 750; }
    .local-badge::before { content: "● "; font-size: .65rem; }
    .chat-setup { display: grid; grid-template-columns: 1fr 1fr auto; gap: 12px; padding: 19px 26px; border-bottom: 1px solid var(--line); }
    .field { display: grid; gap: 7px; }
    label { color: var(--muted); font-size: .76rem; font-weight: 720; }
    select, textarea { width: 100%; border: 1px solid rgba(159,219,143,.24); border-radius: 11px; background: rgba(4,21,12,.64); outline: none; }
    select { min-height: 42px; padding: 0 11px; }
    select:focus, textarea:focus { border-color: var(--leaf); box-shadow: 0 0 0 3px rgba(159,219,143,.11); }
    button { min-height: 42px; padding: 0 15px; border: 1px solid rgba(159,219,143,.34); border-radius: 11px; background: rgba(96,171,82,.15); cursor: pointer; }
    button:hover, button:focus-visible { border-color: var(--leaf); background: rgba(121,196,97,.22); outline: none; }
    button:disabled, select:disabled { cursor: wait; opacity: .55; }
    .refresh-button { align-self: end; white-space: nowrap; }
    .chat-state { min-height: 54px; margin: 0; padding: 15px 26px 0; color: var(--muted); font-size: .88rem; }
    .chat-state.error, .notes-status.error { color: var(--danger); }
    #local-outer-chat { min-height: 258px; padding: 12px 26px 26px; }
    .notes-body { padding: 19px 22px 22px; }
    .system-chat-card { grid-column: 1 / -1; }
    .system-chat-body { padding: 20px 26px 26px; }
    #personal-notes { min-height: 318px; padding: 14px; resize: vertical; line-height: 1.55; }
    .notes-status { min-height: 20px; margin: 10px 0 0; color: var(--faint); font-size: .77rem; }
    .footer { display: flex; justify-content: space-between; gap: 14px; align-items: center; padding-top: 25px; color: var(--faint); font-size: .82rem; }
    @media (max-width: 820px) { .hero, .dashboard { grid-template-columns: 1fr; } .chat-setup { grid-template-columns: 1fr; } .refresh-button { justify-self: start; } }
    @media (max-width: 540px) { .shell { width: min(100% - 26px,1180px); padding-top: 18px; } .topbar { align-items: flex-start; padding-bottom: 15px; } system-navigation { --system-navigation-direction: column; --system-navigation-align: flex-end; --system-navigation-padding: 7px; } .card-head, .chat-setup, #local-outer-chat, .system-chat-body { padding-left: 17px; padding-right: 17px; } .hero { padding-top: 20px; } .footer { align-items: flex-start; flex-direction: column; } }
  </style>
</head>
<body>
  <main class="shell">
    <header class="topbar">
      <a class="brand" href="/" aria-label="Dantes home"><span class="mark" aria-hidden="true">D</span> Dantes</a>
      <system-navigation></system-navigation>
    </header>
    <section class="hero" aria-labelledby="home-heading">
      <div>
        <p class="eyebrow">Private local workspace</p>
        <h1 id="home-heading">A quieter place to <em>think and act.</em></h1>
        <p class="lead">Talk through an intent with the provider-neutral outer AI, keep a private scratchpad, and stay grounded in the moment.</p>
        <p class="lead">Installed application pages appear automatically in the navigation above.</p>
      </div>
      <section class="clock" aria-label="Local date and time"><small>Here and now</small><time id="local-date-time" aria-live="polite"></time></section>
    </section>
    <section class="dashboard" aria-label="Personal dashboard">
      <section class="card" aria-labelledby="chat-heading">
        <header class="card-head">
          <div><span class="card-kicker">Conversation</span><h2 id="chat-heading">Outer AI</h2><p>Choose a discovered provider, model, application, and optional runtime state. Plans and actions retain their normal confirmation.</p></div>
          <span class="local-badge">Provider neutral</span>
        </header>
        <div class="chat-setup">
          <div class="field"><label for="chat-application">Application</label><select id="chat-application" aria-describedby="chat-state"></select></div>
          <div class="field"><label for="chat-state-space">Runtime state (optional)</label><select id="chat-state-space" aria-describedby="chat-state"></select></div>
          <button class="refresh-button" id="refresh-chat-context" type="button">Refresh</button>
        </div>
        <p class="chat-state" id="chat-state" aria-live="polite">Loading available applications…</p>
        <div id="local-outer-chat"></div>
      </section>
      <section class="card" aria-labelledby="notes-heading">
        <header class="card-head"><div><span class="card-kicker">Scratchpad</span><h2 id="notes-heading">Notes</h2><p class="note-copy">Saved only in this browser.</p></div></header>
        <div class="notes-body"><label for="personal-notes">Private notes</label><textarea id="personal-notes" placeholder="Leave yourself a thought, a thread to pick up, or a small plan…"></textarea><p class="notes-status" id="notes-status" aria-live="polite">Notes stay on this device in this browser.</p></div>
      </section>
      <section class="card system-chat-card" aria-labelledby="system-chat-heading">
        <header class="card-head">
          <div><span class="card-kicker">System scope</span><h2 id="system-chat-heading">Inner AI</h2><p>Run messages, structured requests, tasks, plans, recipes, and schedules through registered direct system capabilities.</p></div>
          <span class="local-badge">Direct capabilities</span>
        </header>
        <div class="system-chat-body"><inner-ai aria-label="Inner AI system workspace"></inner-ai></div>
      </section>
    </section>
    <footer class="footer"><span>Local-first personal dashboard</span><a class="quiet-link" href="/ui/control-center/index.html">Open control center</a></footer>
  </main>
  <script type="module" src="/components/system-workspace.js"></script>
  <script>
    (() => {
      const storageKeys = { notes: "dantes.personal-dashboard.notes.v1", chat: "dantes.personal-dashboard.chat.v1" };
      const dateTime = document.querySelector("#local-date-time");
      const notes = document.querySelector("#personal-notes");
      const notesStatus = document.querySelector("#notes-status");
      const application = document.querySelector("#chat-application");
      const stateSpace = document.querySelector("#chat-state-space");
      const chatState = document.querySelector("#chat-state");
      const chat = document.querySelector("#local-outer-chat");
      const refresh = document.querySelector("#refresh-chat-context");
      let chatPreferences = readStoredJson(storageKeys.chat, {});
      const updateClock = () => {
        const now = new Date(); dateTime.dateTime = now.toISOString();
        dateTime.textContent = new Intl.DateTimeFormat(undefined, { weekday: "long", year: "numeric", month: "long", day: "numeric", hour: "2-digit", minute: "2-digit", second: "2-digit" }).format(now);
      };
      updateClock(); window.setInterval(updateClock, 1000);
      try { notes.value = localStorage.getItem(storageKeys.notes) || ""; }
      catch (_) { setNotesStatus("Browser storage is unavailable. This note will not survive a refresh.", true); }
      notes.addEventListener("input", () => {
        try { localStorage.setItem(storageKeys.notes, notes.value); setNotesStatus("Saved locally in this browser.", false); }
        catch (_) { setNotesStatus("Browser storage is unavailable. This note will not survive a refresh.", true); }
      });
      application.addEventListener("change", async () => { rememberChatPreferences({ applicationId: application.value, stateSpaceId: null }); await loadStateSpaces(); });
      stateSpace.addEventListener("change", () => { rememberChatPreferences({ stateSpaceId: stateSpace.value }); mountConversation(); });
      refresh.addEventListener("click", loadApplications);
      async function loadApplications() {
        setChatState("Loading available applications…"); disableChatSetup(true);
        try {
          const applications = await readAll("/api/control/structure/applications");
          fillSelect(application, applications, "id", item => item.displayName || item.id, chatPreferences.applicationId);
          if (!application.value) { chat.replaceChildren(); setChatState("No registered applications are available yet.", true); return; }
          await loadStateSpaces();
        } catch (_) { chat.replaceChildren(); setChatState("Application structure is unavailable. Open the control center to check local access and registered applications.", true); }
        finally { disableChatSetup(false); }
      }
      async function loadStateSpaces() {
        if (!application.value) return;
        setChatState("Loading state spaces…"); stateSpace.disabled = true;
        try {
          const path = "/api/control/structure/applications/" + encodeURIComponent(application.value) + "/state-spaces";
          const spaces = await readAll(path);
          const currentRuntimeSpaces = spaces.filter(item => item.scope === "runtime" && item.isCurrent === true);
          stateSpace.replaceChildren();
          const applicationOnly = document.createElement("option");
          applicationOnly.value = ""; applicationOnly.textContent = "Application context only";
          stateSpace.append(applicationOnly);
          for (const value of currentRuntimeSpaces) {
            const option = document.createElement("option"); option.value = value.stateSpaceId;
            option.textContent = value.stateSpaceId; stateSpace.append(option);
          }
          const preferred = currentRuntimeSpaces.find(value => value.stateSpaceId === chatPreferences.stateSpaceId);
          stateSpace.value = preferred?.stateSpaceId || currentRuntimeSpaces[0]?.stateSpaceId || "";
          rememberChatPreferences({ applicationId: application.value, stateSpaceId: stateSpace.value || null });
          mountConversation();
          if (!currentRuntimeSpaces.length) setChatState("Ready for an application-aware outer chat. No currently bound runtime state is available, so campaign tools stay out of scope.");
        } catch (_) { chat.replaceChildren(); setChatState("State spaces are unavailable for this application. Refresh or inspect it in the control center.", true); }
        finally { stateSpace.disabled = false; }
      }
      function mountConversation() {
        if (!application.value) return;
        chat.replaceChildren();
        const conversation = document.createElement("outer-ai");
        conversation.setAttribute("application-id", application.value);
        if (stateSpace.value) conversation.setAttribute("state-space-id", stateSpace.value);
        conversation.addEventListener("ai-error", event => setChatState(event.detail.message, true));
        conversation.addEventListener("ai-progress", () => setChatState("Outer AI is working…"));
        conversation.addEventListener("ai-result", () => setChatState("The AI result is ready for review."));
        chat.append(conversation); setChatState("Ready for a provider-neutral outer AI conversation.");
      }
      async function readAll(path) {
        const values = []; let next = path + (path.includes("?") ? "&" : "?") + "limit=100";
        while (next) {
          const response = await fetch(next, { cache: "no-store", credentials: "same-origin" });
          if (!response.ok) throw new Error("structure-unavailable");
          const page = await response.json(); values.push(...(page.items || []));
          next = page.nextCursor ? path + (path.includes("?") ? "&" : "?") + "limit=100&cursor=" + encodeURIComponent(page.nextCursor) : null;
        }
        return values;
      }
      function fillSelect(select, values, id, label, preferred) {
        select.replaceChildren();
        for (const value of values) { const option = document.createElement("option"); option.value = value[id]; option.textContent = label(value); select.append(option); }
        const match = values.find(value => value[id] === preferred); if (match) select.value = match[id];
      }
      function rememberChatPreferences(change) {
        chatPreferences = { ...chatPreferences, ...change }; if (!chatPreferences.sessionContextId) chatPreferences.sessionContextId = createSessionContext();
        try { localStorage.setItem(storageKeys.chat, JSON.stringify(chatPreferences)); } catch (_) { }
      }
      function createSessionContext() {
        const random = crypto.randomUUID ? crypto.randomUUID().replaceAll("-", "") : Date.now().toString(36);
        const sessionContextId = "home-session." + random; chatPreferences.sessionContextId = sessionContextId; return sessionContextId;
      }
      function readStoredJson(key, fallback) { try { return JSON.parse(localStorage.getItem(key) || "null") || fallback; } catch (_) { return fallback; } }
      function disableChatSetup(disabled) { application.disabled = disabled; stateSpace.disabled = disabled; refresh.disabled = disabled; }
      function setChatState(message, isError = false) { chatState.textContent = message; chatState.classList.toggle("error", isError); }
      function setNotesStatus(message, isError) { notesStatus.textContent = message; notesStatus.classList.toggle("error", isError); }
      loadApplications();
    })();
  </script>
</body>
</html>
