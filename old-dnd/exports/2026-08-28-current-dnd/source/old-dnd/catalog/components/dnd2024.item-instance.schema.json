{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "D&D 2024 physical item instance",
  "type": "object",
  "additionalProperties": false,
  "required": ["definitionId"],
  "properties": {
    "definitionId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9.-]*\\.v[1-9][0-9]*$",
      "maxLength": 200,
      "description": "Exact immutable catalog definition entity id. The version is part of the referenced id; no copied definition hash, weight, capacity, source, owner, or quantity belongs here."
    }
  },
  "$comment": "Governed by procedure.mechanic.dnd2024.item-instance. Containment, not an ownerId or inventory array, is an instance's custody."
}
