{
  "type": "object",
  "additionalProperties": false,
  "required": ["subjectId", "encounterId", "sourceRef"],
  "properties": {
    "subjectId": {"type": "string", "minLength": 1, "maxLength": 200},
    "encounterId": {"type": "string", "minLength": 1, "maxLength": 200},
    "sourceRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sourceId", "locator"],
      "properties": {
        "sourceId": {"const": "source.dnd2024.srd-5.2.1"},
        "locator": {"const": "Playing the Game > Combat > The Order of Combat > Initiative"}
      }
    }
  },
  "x-dantes-entity-payload-fields": ["subjectId"]
}
