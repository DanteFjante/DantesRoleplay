# Application kernel completion receipt

Accepted: **2026-08-25**  
Ruleset alignment: **ruleset-neutral**  
Owner: [Generic application-kernel dependency plan](../APPLICATION-KERNEL-DEPENDENCY-PLAN.md)

## Delivered boundary

- `system.*` owns generic administration while registered applications own non-system component,
  catalog, mechanic, and action contracts.
- Registered allowed directory/path/glob sources resolve deterministic trust-aware overlays before
  catalog navigation, retrieval, AI inspection, or execution.
- The application-scoped ECS stores every bounded schema-approved JSON kind without application CLR
  types or per-game database columns. Exact immutable type versions and hashes guard every write.
- Versioned structural projections declare exact component fields and projection dependencies,
  reject cycles and hidden cross-application edges, materialize bounded committed state, and expose
  deterministic reverse-impact evidence.
- Effective catalogs are described, traversable, exactly inspectable, cursor-paginated, and
  deterministically searchable without vectors or local AI.
- Local and remote planners consume the same effective application contracts through a common
  verifier. Models cannot register, activate, validate, migrate, authorize, or execute by assertion.
- Generic projects no longer compile the retained application/game-adapter C# trees. The system and
  local AI contain no game-pack dependency; retained files remain on disk by explicit user choice.

## Acceptance evidence

- Application-kernel Slices 0–11J and interaction-orchestration Slices 12A–12H have accepted
  implementation documents and receipts.
- The final Slice 12H matrix passed **136 focused tests**, **787 shared tests**, and **20 standalone
  local-AI tests**, with zero failures.
- Protocol evidence passed 6 live rows with 2 deliberately retired authored-procedure paths skipped.
- Catalog validation accepted 144 records with existing advisory-only near-duplicate warnings and
  explicitly did not touch live data.
- Migration consistency passed, the isolated solution build produced 0 warnings and 0 errors, and
  architecture checks found no generic application/game-adapter compile wildcard or local-AI game
  dependency.
- `git diff --check` passed with line-ending notices only.

## Deliberate first-delivery exclusions

- A non-empty state space upgrades only when separately reviewed compatibility/migration evidence
  exists. The current endpoint fails safely with `MIGRATION_REQUIRED`; it never coerces, discards,
  or silently reinterprets stored JSON.
- Structural projections recompute from canonical component state. A revision-keyed disposable
  output cache is an optional performance enhancement, never required for correctness or authority.
- E7 continues to own projections over uncommitted root-local virtual effects.
- Compatibility aliases and retained uncompiled legacy source files are not destructively removed.
- Application-specific catalog expansion and game mechanics remain application work, not kernel
  completion criteria.

The user's 2026-08-25 instruction to finish the kernel upgrade confirms final acceptance of this
bounded first delivery. The normal local host database was not initialized, migrated, imported, or
otherwise changed by this closure.
