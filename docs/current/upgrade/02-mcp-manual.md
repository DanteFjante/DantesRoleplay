# Extensive, intent-selected MCP manual

Owner: root agent final_guide_alignment. Worktree: C:/repo/DantesRoleplay-platform-manual, DETACHED from 9395cd8e; incorporate the prepared master baseline and acceptance corrections before final integration.

## Outcome

A newly connected AI learns how to discover and operate the system without guessing APIs or reading the whole catalog. Keep orientation short and route intent to extensive canonical procedures. Each deep procedure provides prerequisites, current capability/schema discovery, exact input examples, host-owned versus caller-owned fields, results and receipts, permissions, limits, stale/conflict handling, cancellation, replay, and recovery.

The actual public MCP verbs remain orient/query/commit plus registered direct capability tools. Never invent a verb or claim a worker summary is a committed mutation.

## Approved work

Improve the existing system.use bootstrap, system.inspect, action.run, mechanic.run, information/event instructions, eight application-candidate procedures, five INNER procedures, and standing-grant administration. Add Matches intent associations and links so retrieval can select the relevant section. Preserve existing canonical IDs rather than creating duplicates.

Four new canonical procedure IDs are approved for uncovered owners:

- procedure.system.application-lifecycle
- procedure.system.runtime-services
- procedure.system.trigger-scheduling
- procedure.system.web-composition

Own their namespace declarations and exact manual tests. Approved shared C# metadata change: only procedure-reference arrays in DantesRoleplay.MCPServer/Mcp/McpVerbCatalog.cs for existing lifecycle and trigger descriptors. No verb, descriptor ID, input schema, behavior, or parallel registry change.

Cover runtime services, durable handoff, generation/cache behavior, application/schema/state-space lifecycle, scheduled/conditional observers, INNER dependency mapping, and web composition/publication/action boundaries. Explain supported authoring grammars and unsupported schema conversion/new-query cases accurately. Incorporate theme instructions only once the shared website module exists.

## Dependencies and safeguards

The verification owner corrects the submit manual's line-24 placeholder after its frozen run. Preserve that correction when integrating the expanded manual. Correct the cancel procedure's obsolete claim that list/wait are unavailable.

Shared theme contract: /components/system-theme.js, preference dantes.system-theme.v1 with system/light/dark, data-system-theme on the document element, semantic --system-color-* variables, and system-theme-toggle. This is presentation, not an authority or runtime theme-authoring API.

## Acceptance

Demonstrate cold orientation and intent/manual retrieval for authoring, jobs/observers, and web tasks. Use actual canonical records and deterministic retrieval; retain fallback when embeddings are unavailable. Validate authored schemas/examples against existing descriptors, the MCP contract guard, and catalog validation. Use controlled providers only where needed and state that limit. Run focused tests; the coordinator owns broad final acceptance.

## Current checkpoint

Coverage audit and the four IDs/shared metadata boundary are approved. Implementation is assigned; no expanded-manual commit or verification result has been delivered yet. Previous guide-only alignment 497b212c is a separate completed change awaiting integration. Next: write canonical procedures, link descriptor references, run focused cold-start/guard/catalog checks, and record the tested commit here.
