# Local deployment and database activation

The user explicitly authorized finishing the implementation, loading the changes onto master and into the database, cleanup, and starting the system so they can try it on 2026-09-12. This authorizes the existing supported backup, rehearsal, migration, catalog synchronization, application/page activation, and local release-selection operations needed for this upgrade. Preserve live gameplay and runtime-authored records; do not reset the database or replace it with fixtures.

## Ownership and order

Coordinator owns master, shared decisions, this plan, and final browser verification. Agent `website_integration` prepares database/release operations and receives exclusive deployment write ownership from the coordinator before live mutations. Agent `workflow_publication_finish` investigates the website/application publication path read-only. One writer performs release and database changes.

1. Merge the accepted combined implementation onto master and verify its source matches the tested revision.
2. Resolve the saved runtime launch profile, actual running process, database/blob paths, selected application/page, and existing audience policy. Do not guess a release from the newest directory or development defaults.
3. Use consistent SQLite backup and blob preservation; export corresponding database-authored records and compare them with reviewed catalog files before synchronization. Keep an independently verified recovery point and use a separate copy for rehearsal.
4. Build/package the exact accepted source, rehearse migrations and reviewed catalog/application/page changes against copied state, and verify retained state and application readiness.
5. Apply the proven operations to the live database, activate the intended application/state-space/page revisions where necessary, select the new pinned runtime release, and restart only its identified managed process.
6. Verify the normal launcher, actual site/API/MCP readiness, navigation, theme asset, DND page, and at least one real authorized read. Preserve existing external access settings; do not expand them.
7. Record exact release/source/database/backup paths, selected revisions, results, known limits, and recovery steps. Keep previous release and matching backup. Remove only proven disposable task artifacts; never remove active release directories, blobs, work, or needed toolchains.

## Current checkpoint

Combined implementation is accepted at `0a445b54d4f78b98332b789adfbb7ed2833f2e25`: full suite 3,456 passed, zero failed/skipped; opt-in protocol eight passed with two intentional retired cases skipped; build and browser/catalog checks passed. It was merged onto actual master at `ef73bab1a84ee11abf002f7c8d496500b04f1c4c`, with all non-documentation files matching the accepted implementation.

Local branch cleanup is already complete: only master exists; remote refs, both stashes, and all worktree directories were preserved. Root granted agent `website_integration` exclusive deployment write ownership, and the live upgrade below has now been applied through supported synchronization, CAS, and launcher boundaries.

The current byte-verified saved profile is `DantesRoleplay.MCPServer/data/runtime-launch.json` (schema 1, SHA-256 `C823BEAF6E905BC1136CA22D95CF70731D9FFC17C2F6D3E96512213C03DD3944`). It selects the live database `DantesRoleplay.MCPServer/data/dantesroleplay.db`, sibling `blobs`, application `dnd2024`, port 6217, and the existing localhost/public origins and anonymous-public policy. It pins 23 files in the accepted host release and 4,593 files in the accepted catalog source release. The launcher owns PID 27432 on `0.0.0.0:6217` and verified all three configured origins.

Recovery root `DantesRoleplay.MCPServer/data/backups/platform-upgrade-20260912T113843Z` contains the verified 91,033,600-byte online backup (SHA-256 `46BA82A3A765D1D2C413F40BD1A283F906CD2AFDD3452A8BFF5FDA3372DB0BEE`), a full 603-record pre-upgrade catalog export, and a byte-verified copy of all 152 blobs. It also contains the final 621-record live export at `live-catalog-post-upgrade` (manifest SHA-256 `95467299B18936A571DCCFA794F79BAE298B43646DCAA4EDA27C56FF15A7CDC8`). Exact deployables are `DantesRoleplay.MCPServer/data/releases/platform-0a445b54-20260912-host` and `...-source`; their folder names retain the accepted `0a445b54` base label, while the published DND bundle also contains the reviewed master `b5c0e802` UI correction.

Rehearsal used the separate copied database and blobs beneath the recovery root and was served only at `http://127.0.0.1:16217`, with completion, remote planning, embedding, and Ollama dispatch disabled. Its listener and process are now stopped. Startup applied all ten kernel and two web migrations after namespace-only synchronization created the five required namespaces. Startup then seeded the accepted `procedure.system.use` and new manuals with prior history retained. The ordinary catalog import updated only `system.web.page`; it preserved the two live-authored item edits and all three live-only records.

The exact source preview was valid with zero problems. Dry-run and CAS commit advanced DND activation 65 (`0025E78D08A8EE4B3281DBD95B9A9C844541584FAC663E7C32E127585C3CD0C1`) to 66 (`717BE58BCC02FE2A2F2727D3227E43C45086E2AFE93577F4B1A4695F9CB9A45B`). The compatible state-space dry-run and commit advanced `dnd2024-main` binding 39 (`DD290583646309C8AC826D2CBC19689DE40E07F41797C92BEB94539C75C98B5D`) to 40 (`7B8620E3AC16679641B3934B903120745FD3BBF342D344AE1D7501DBCD059DFA`) without replacing state. Web migration recognized the existing reviewed `dnd2024-play` application identity and both system-owned pages with content verified, so no duplicate identity review was submitted.

The first rebuilt DND bundle contains 63 files and 1,046,572 uncompressed bytes; ZIP SHA-256 is `3EFD73935DF8FF738B13CD86456753495944D92824608DA69860CEBC68D4BC92`. All stored bytes matched the build before page CAS advanced 77 to 78 with content hash `B522E5253F4A14281EEF9DB357E079002A4FBD47A9EE76D6D4F48F3464491C85`. The host-admin helper dry-ran and advanced system-owned `home` 8 to 9 with content hash `1BDFA7964854AB0B7B569341BDA5D3B44003F378249668A7AC75A5AFDA0A566A`; `control-center` remains 10. Rehearsal readiness is HTTP 200 `ready`, audience remains bound to `dnd2024-main` / `game-master`, and root, DND page, both shared browser modules, the published-page descriptor, and a real campaign entity read return HTTP 200. Exact receipts are retained in the rehearsal directory.

Browser review found and corrected a duplicate theme control and obsolete shared-table perspective switch. The reviewed fix is master `b5c0e802`; its clean-build ZIP is 300,337 bytes with 63 entries and SHA-256 `F59A6D93B7AB3DA6593F2A3EAC41F8100E653F9719E405AE9EC40B44A2C0402D`. Application preview remained byte-identical and valid, confirming that no second application/state-space activation was warranted. Every stored bundle byte matched before CAS advanced the rehearsal page 78 to 79 with content hash `B213F7E4DEB440641AD034AC8659C336B31830E1C914685BC667A3A9A734D5FB`; readiness remained HTTP 200 `ready` at activation 66 and binding 40. Browser verification passed with one theme selector, full-width navigation, shared-table labeling, Lore in light and dark themes, complete 35-record Faction continuation, and no browser warnings or errors.

Live synchronization created only the same five namespaces. Startup applied all ten kernel and two web migrations, leaving the live database at 85 kernel migrations through `20260912081912_DurableConditionalWorkflowObservers` and six web migrations through `20260911193954_WebPageResourceIdentity`. A fresh 621-record post-bootstrap export was retained; a separate import workset replaced only the accepted `system.web.page` files, and dry-run plus import updated that one record while leaving every other record unchanged. The source preview had zero problems. Dry-run and CAS produced the same activation 66 and `dnd2024-main` binding 40 fingerprints proven in rehearsal. The already-reviewed page identity remained content-verified. Home advanced 8 to 9, and all 63 corrected bundle entries were byte-verified before DND page CAS advanced 77 to 78 with content hash `B213F7E4DEB440641AD034AC8659C336B31830E1C914685BC667A3A9A734D5FB`; `control-center` remains 10.

The live database now reports `integrity_check=ok`, zero foreign-key violations, and remains paired with the retained pre-upgrade database/export/blob recovery point. Only the launcher-owned PID 27432 listens on `0.0.0.0:6217`; no rehearsal listener remains. Readiness and audience calls return HTTP 200 from `127.0.0.1`, `localhost`, and the existing public origin; the saved policy remains `shared` / `game-master` on `dnd2024-main`. Root, DND page, shared theme/workspace modules, published-page descriptor, and a real campaign entity read return HTTP 200. Live MCP manual retrieval also passed for `procedure.system.use` v22 and the three new v1 manuals. The first managed process served a damaged `ai-workspace.js` response even though pinned disk matched accepted source; a supported owned-process restart restored matching responses without source or release-file edits. The cause of the damaged response was not established. All 11 shared JavaScript files now match accepted source, pinned disk, and fresh HTTP bytes and pass Node syntax checks.

Final live browser acceptance passed after that restart: home navigation upgraded, the application chooser opened DND, one theme control and the shared-table header rendered, real Lore cards loaded, Factions continued from 25 to all 35 records and removed the completed control, both light and dark themes were readable, and the browser console remained free of warnings and errors. The theme was returned to System.

For recovery, keep the old release and the `.previous` saved-profile copies with the pre-upgrade database and matching blob backup. A host-only rollback may select an older compatible profile only after checking it against the current migrated database; do not run the old host blindly against the upgraded schema. A database rollback requires stopping the launcher-owned process, preserving any post-upgrade gameplay/export first, restoring the verified pre-upgrade database together with its matching 152-blob backup, selecting the matching old host/source profile, and starting through `run-mcp-server.ps1`. Workers may update this checkpoint in the original checkout but must not stage or commit there; the coordinator serializes Git changes.
