var exposure=ctx.roles.exposure,definition=ctx.roles.definition,subject=ctx.roles.subject,i=ctx.input,LINK='dnd2024.core.definition-link',STATE='dnd2024.hazard.environment-exposure',VERSION='dnd2024.core.version',SUBJECT='hazard.exposure.subject',RECOVERED='hazard.exposure.recovered';function object(v){return v!==null&&!Array.isArray(v)&&typeof v==='object';}function parse(v,l){try{var x=typeof v==='string'?JSON.parse(v):v;if(!object(x))throw 0;return x;}catch(e){throw new Error(l+' is malformed.');}}function ref(v){return object(v)&&typeof v.entityId==='string'&&v.entityId.length>0;}function source(v){return object(v)&&Array.isArray(v.citations)&&v.citations.length>0&&ref(v.citations[0].sourceRef)&&typeof v.citations[0].locator==='string'?{sourceId:v.citations[0].sourceRef.entityId,locator:v.citations[0].locator}:null;}var link=parse(exposure.components[LINK],'Exposure definition link'),state=parse(exposure.components[STATE],'Exposure state'),version=parse(definition.components[VERSION],'Environmental version'),rows=exposure.relationships||[],owned=false,recovered=false;for(var n=0;n<rows.length;n++){if(rows[n].fromEntityId===exposure.id&&rows[n].toEntityId===subject.id&&rows[n].kind===SUBJECT)owned=true;if(rows[n].fromEntityId===exposure.id&&rows[n].toEntityId===subject.id&&rows[n].kind===RECOVERED)recovered=true;}if(!ref(link.definition)||link.definition.entityId!==definition.id||link.definitionRevision!==version.revision||version.status!=='active'||version.revision!==i.expectedDefinitionRevision||!owned||recovered||!Number.isSafeInteger(state.exposureCount)||!Number.isSafeInteger(state.failedChecks))throw new Error('Environmental exposure is unavailable, stale, or already recovered.');var provenance=source(parse(definition.components['dnd2024.core.source'],'Environmental source'));if(!provenance)throw new Error('Environmental source provenance is invalid.');return {narration:subject.name+' recovers from '+definition.name+'.',effects:[{type:'relationship.create',entityId:exposure.id,toEntityId:subject.id,kind:RECOVERED,data:'{}'}],events:[{type:'dnd2024.hazard.exposure-recovered',payload:{contractVersion:1,exposureId:exposure.id,subjectId:subject.id,definitionId:definition.id,definitionRevision:version.revision,exposureCount:state.exposureCount,failedChecks:state.failedChecks,sourceRef:provenance},entityIds:[exposure.id,subject.id,definition.id],scope:subject.id}],notifications:[],data:{test:'environment-exposure-recover',exposureId:exposure.id,subjectId:subject.id,recovered:true,exposureCount:state.exposureCount,failedChecks:state.failedChecks,sourceRef:provenance}};
